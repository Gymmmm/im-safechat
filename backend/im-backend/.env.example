# Database Configuration
DB_HOST=localhost
DB_USER=fcim
DB_PASSWORD=MyNew@2025Safe
DB_NAME=chat_app

# JWT Secret Key (change in production)
JWT_SECRET=your_jwt_secret_change_in_production

# Server Port
PORT=3001

# Admin Panel Access (set to 'true' to enable)
ENABLE_BOSS=false
